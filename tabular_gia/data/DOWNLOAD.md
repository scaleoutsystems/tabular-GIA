Download the datasets given the shell script download_datasets.sh

How to use shell script:

Make sure you are in python virtual env
```bash
chmod +x download_datasets.sh
./download_datasets.sh data
```