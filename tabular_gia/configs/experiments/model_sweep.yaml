experiment: model_sweep

fl:
  protocol: fedsgd
  rounds: 25
  clients_per_round: 10
  local_epochs: 1
  normalization: client
  iid: true

model:
  name: tabular_mlp
  depth: [2, 3, 4]
  width: [64, 128]
  dropout: [0.0, 0.1]

attack:
  name: inverting_gradients
  steps: 1000
  lr: 0.03
